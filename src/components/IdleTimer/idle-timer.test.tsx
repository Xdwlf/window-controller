import React from 'react';
// import { timeToMs } from '.';

describe('timeToMs', () => {
    test('timeToMs should return correct milliseconds based on time', () => {
        //     expect(timeToMs('12:00')).toBe(43200000);
        //     expect(timeToMs('12:10')).toBe(43800000);
        //     expect(timeToMs('12:05')).toBe(43500000);
        //     expect(timeToMs('01:00')).toBe(3600000);
        //     expect(timeToMs('00:20')).toBe(1200000);
        //     expect(timeToMs('01:20')).toBe(4800000);
    });
    // test('timeToMs should return zero for bad input', () => {
    //     expect(timeToMs('Time:ToEat')).toBe(0);
    //     expect(timeToMs('12:Oclock')).toBe(0);
    //     expect(timeToMs('Spongebob:12')).toBe(0);
    // });
});
